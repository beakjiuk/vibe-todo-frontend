# todo-backend API 베이스 (반드시 /api 까지)
# 로컬
VITE_API_BASE=http://localhost:5000/api

# Heroku 배포 백엔드로 쓸 때 예시 (위 줄 대신)
# VITE_API_BASE=https://YOUR-APP.herokuapp.com/api

# JWT는 HttpOnly 쿠키로 전달됩니다. 백엔드의 FRONTEND_ORIGIN(Heroku는 Config Vars)을
# 이 앱의 출처(예: http://localhost:5173)와 맞춰야 로그인·쿠키가 동작합니다.
